<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MovieAddListVo">
	<insert id="insertMovie">
		INSERT INTO MOVIE_ADDLIST_BOARD
		(TITLE, THEATER, SUB_TITLE, PUB_DATE, DIRECTOR, ACTOR, USER_RATING, IMAGE, SEQUENCE)
		VALUES(#{TITLE}, #{THEATER}, #{SUB_TITLE}, #{PUB_DATE}, #{DIRECTOR}, #{ACTOR}, 
		#{USER_RATING}, #{IMAGE}, MOVIE_ADDLIST_BOARD_SEQ.NEXTVAL)
	</insert>
	
	<delete id="deleteMovie">
		DELETE FROM MOVIE_ADDLIST_BOARD WHERE TITLE = #{TITLE}
	</delete>
	
	<select id="listMovies">
		SELECT TITLE, SUB_TITLE, PUB_DATE, DIRECTOR, ACTOR, USER_RATING, IMAGE
		FROM MOVIE_ADDLIST_BOARD
	</select>
</mapper>